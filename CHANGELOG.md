# Changelog

本项目的显著变更都会记录在此文件。

## [Unreleased]

### Added
- PR 构建与测试（GitHub Actions CI）
- 启动时静默检查更新：每天最多一次，发现新版本后通过通知中心提示
- 菜单栏在休息期间显示剩余时间（如 "Break 00:12"）
- `CHANGELOG.md` 维护发布说明

### Changed
- 升级到 Swift 6 语言模式（严格并发）
- 语言切换重启改为有序退出，避免 `open -n` 造成双实例并存
- `@AppStorage`/`UserDefaults` 键名集中为 `PreferencesKey` 常量
- `make install` 先移入废纸篓再安装，不再直接 `rm -rf`

## [1.6.0] - 2026-08-12

### Added
- 自动休息前 5 秒预告倒计时，可取消
- 鼠标不活动暂停的间隔可配置（1–60 分钟）
- 单元测试 target（版本比较、时间格式、站立休息调度、语言解析）
- `make version VERSION=x.y.z` 版本号自动化

### Changed
- 工作计时改为基于目标时间，睡眠/App Nap 后不再漂移
- 休息遮罩倒计时由控制器单一驱动，结束动作不再重复触发
- 站立休息只统计自动触发的眼部休息
- 鼠标不活动检测覆盖键盘/滚轮/点击/拖动
- 休息期间显示器热插拔自动重建遮罩
- 健康小贴士随机轮换
- 更新下载增加 SHA-256 校验，下载完成后不再自动打开 DMG
- 偏好设置窗口统一为单个窗口
